{
  "root": "server",
  "build": {
    "builder": "pip",
    "command": "pip install -r requirements.txt"
  },
  "start": {
    "command": "gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
